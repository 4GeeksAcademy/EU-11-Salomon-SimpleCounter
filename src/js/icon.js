import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { Clock } from '@fortawesome/fontawesome-free-regular/Clock';

fontawesome.config = {
  autoReplaceSvg: 'nest'
};
fontawesome.library.add(Clock);